# UC6- Use Case 6
This branch consists of a Java File that prints the word "OOPS" with a star pattern using various static methods and classes. Each letter's star pattern is defined in a static method which is then called to print that letter.
